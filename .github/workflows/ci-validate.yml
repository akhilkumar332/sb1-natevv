name: CI Validate

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ci-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            src/functions/package-lock.json
            scripts/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        # Guard against the runner hanging after all suites report: fail the job
        # fast instead of holding a runner for the default 6h job timeout.
        timeout-minutes: 15
        run: npm run test:run

      - name: Build
        env:
          APP_ENVIRONMENT: ci
          APP_DEPLOY_TARGET: firebase-hosting
        run: npm run build
