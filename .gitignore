# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
playwright-report/
test-results/

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
#.firebase/
#.firebaserc
#firebase-debug.log

# env files
.env
.env.*
!.env.example
#vite-env.d.ts

# generated firebase config
public/firebase-config.js

# secrets
secrets/
**/firebase-admin-sdk.json

# project docs artifacts
docs/
documentation/

GEMINI.md
AGENTS.md
workflow
workflow/*

.gemini/
gha-creds-*.json

# Cloud-sync conflict duplicates ("file (1).ext", "file (2).ext").
# These are stale copies, and some of them ('.env (1)', 'src/functions/.env (1).*')
# carry real credentials that the plain `.env` / `.env.*` rules do not match.
*\ (1)*
*\ (2)*
*\ copy*
*\ Copy*
