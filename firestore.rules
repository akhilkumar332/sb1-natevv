rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ========================================================================
    // HELPER FUNCTIONS
    // ========================================================================

    /**
     * Check if user is authenticated
     */
    function isAuthenticated() {
      return request.auth != null;
    }

    /**
     * Check if user owns the resource
     */
    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    /**
     * Get user document data (with existence check)
     */
    function getUserData() {
      return exists(/databases/$(database)/documents/users/$(request.auth.uid))
        ? get(/databases/$(database)/documents/users/$(request.auth.uid)).data
        : null;
    }

    /**
     * Check if user has a specific role
     */
    function hasRole(role) {
      let userData = getUserData();
      return isAuthenticated() && userData != null && userData.role == role;
    }

    /**
     * Check if user is admin
     */
    function isAdmin() {
      return hasRole('admin');
    }

    /**
     * Check if user is donor
     */
    function isDonor() {
      return hasRole('donor');
    }

    /**
     * Check if user is a bloodbank (legacy hospital supported)
     */
    function isHospital() {
      return hasRole('bloodbank') || hasRole('hospital');
    }

    /**
     * Check if user is staff of a bloodbank (parentHospitalId)
     */
    function isHospitalStaffFor(hospitalId) {
      let userData = getUserData();
      return isAuthenticated() && userData != null && userData.parentHospitalId == hospitalId;
    }

    /**
     * Check if user can edit hospital resources
     */
    function hasHospitalEditAccess(hospitalId) {
      let userData = getUserData();
      return isAuthenticated() && (
        request.auth.uid == hospitalId ||
        (
          userData != null &&
          userData.parentHospitalId == hospitalId &&
          userData.staffRole in ['editor', 'manager']
        )
      );
    }

    /**
     * Check if user can manage hospital resources
     */
    function hasHospitalManagerAccess(hospitalId) {
      let userData = getUserData();
      return isAuthenticated() && (
        request.auth.uid == hospitalId ||
        (
          userData != null &&
          userData.parentHospitalId == hospitalId &&
          userData.staffRole == 'manager'
        )
      );
    }

    /**
     * Check if user is NGO
     */
    function isNGO() {
      return hasRole('ngo');
    }

    /**
     * Check if user is verified (for bloodbanks and NGOs)
     */
    function isVerified() {
      return isAuthenticated() && getUserData().verified == true;
    }

    /**
     * Check if user account is active
     */
    function isActive() {
      return isAuthenticated() && getUserData().status == 'active';
    }

    /**
     * Validate referral status updates from referred users
     */
    function isValidReferralStatusUpdate() {
      return request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'updatedAt'])
        && request.resource.data.status in ['registered', 'onboarded', 'eligible', 'deleted']
        && exists(/databases/$(database)/documents/users/$(resource.data.referredUid))
        && (
          request.resource.data.status == 'registered'
          || (
            request.resource.data.status == 'onboarded'
            && get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.onboardingCompleted == true
          )
          || (
            request.resource.data.status == 'eligible'
            && get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.onboardingCompleted == true
            && get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.status != 'deleted'
            && (
              (resource.data.referredAt != null
                && request.time >= resource.data.referredAt + duration.value(7, 'd'))
              || (resource.data.referredAt == null
                && get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.createdAt != null
                && request.time >= get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.createdAt
                  + duration.value(7, 'd'))
            )
          )
          || (
            request.resource.data.status == 'deleted'
            && get(/databases/$(database)/documents/users/$(resource.data.referredUid)).data.status == 'deleted'
          )
        );
    }

    /**
     * Allow donors to expire accepted donor requests after the contact window ends.
     */
    function isDonorRequestExpiryUpdate() {
      return request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'expiredAt', 'updatedAt'])
        && request.resource.data.status == 'expired'
        && (
          // Accepted request: expire after contact window.
          (resource.data.status == 'accepted' &&
            (
              (resource.data.connectionExpiresAt != null && request.time >= resource.data.connectionExpiresAt)
              || (resource.data.connectionExpiresAt == null && resource.data.respondedAt != null
                && request.time >= resource.data.respondedAt + duration.value(24, 'h'))
            )
          )
          // Pending request: expire after 24 hours from requestedAt.
          || (resource.data.status == 'pending'
            && resource.data.requestedAt != null
            && request.time >= resource.data.requestedAt + duration.value(24, 'h'))
        );
    }

    /**
     * Allow referral notifications only if the auth user is the referred donor
     */
    function isReferralNotificationCreate() {
      return request.resource.data.type == 'referral'
        && request.resource.data.referralId is string
        && exists(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId))
        && get(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId)).data.referrerUid
          == request.resource.data.userId
        && get(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId)).data.referredUid
          == request.auth.uid;
    }

    /**
     * Allow referrer to create referral notifications for themselves
     */
    function isReferralNotificationForReferrer() {
      return request.resource.data.type == 'referral'
        && request.resource.data.referralId is string
        && exists(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId))
        && get(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId)).data.referrerUid
          == request.auth.uid
        && get(/databases/$(database)/documents/ReferralTracking/$(request.resource.data.referralId)).data.referrerUid
          == request.resource.data.userId;
    }

    /**
     * Allow users to create their own general notifications (client-side FCM bridge).
     */
    function isSelfNotificationCreate() {
      return request.resource.data.userId == request.auth.uid
        && request.resource.data.type in [
          'emergency_request',
          'donor_request',
          'appointment_reminder',
          'campaign_invite',
          'donation_confirmation',
          'verification_status',
          'achievement',
          'referral',
          'general'
        ]
        && request.resource.data.title is string
        && request.resource.data.message is string
        && request.resource.data.read == false
        && request.resource.data.priority in ['low', 'medium', 'high']
        && request.resource.data.createdAt is timestamp;
    }

    /**
     * Donor -> hospital appointment updates
     */
    function isAppointmentNotificationForHospital() {
      return request.resource.data.relatedType == 'appointment'
        && request.resource.data.relatedId is string
        && exists(/databases/$(database)/documents/appointments/$(request.resource.data.relatedId))
        && get(/databases/$(database)/documents/appointments/$(request.resource.data.relatedId)).data.donorId
          == request.auth.uid
        && get(/databases/$(database)/documents/appointments/$(request.resource.data.relatedId)).data.hospitalId
          == request.resource.data.userId;
    }

    /**
     * Donor -> requester updates for blood requests
     */
    function isBloodRequestNotificationForRequester() {
      return request.resource.data.relatedType == 'blood_request'
        && request.resource.data.relatedId is string
        && exists(/databases/$(database)/documents/bloodRequests/$(request.resource.data.relatedId))
        && get(/databases/$(database)/documents/bloodRequests/$(request.resource.data.relatedId)).data.requesterId
          == request.resource.data.userId
        && get(/databases/$(database)/documents/bloodRequests/$(request.resource.data.relatedId)).data.respondedDonors != null
        && request.auth.uid in get(/databases/$(database)/documents/bloodRequests/$(request.resource.data.relatedId)).data.respondedDonors;
    }

    /**
     * Donor -> requester updates for donor-to-donor requests
     */
    function isDonorRequestNotificationForRequester() {
      return request.resource.data.relatedType == 'donor_request'
        && request.resource.data.relatedId is string
        && exists(/databases/$(database)/documents/donorRequests/$(request.resource.data.relatedId))
        && get(/databases/$(database)/documents/donorRequests/$(request.resource.data.relatedId)).data.targetDonorUid
          == request.auth.uid
        && get(/databases/$(database)/documents/donorRequests/$(request.resource.data.relatedId)).data.requesterUid
          == request.resource.data.userId
        && get(/databases/$(database)/documents/donorRequests/$(request.resource.data.relatedId)).data.status
          in ['accepted', 'rejected'];
    }

    /**
     * Donor -> self appointment reminders
     */
    function isAppointmentNotificationForDonor() {
      return request.resource.data.relatedType == 'appointment'
        && request.resource.data.relatedId is string
        && exists(/databases/$(database)/documents/appointments/$(request.resource.data.relatedId))
        && get(/databases/$(database)/documents/appointments/$(request.resource.data.relatedId)).data.donorId
          == request.auth.uid
        && request.resource.data.userId == request.auth.uid;
    }

    // ========================================================================
    // COLLECTION: users
    // ========================================================================

    match /users/{userId} {
      // Anyone authenticated can read user data
      allow read: if isAuthenticated();

      // Users can create their own document during registration
      allow create: if isAuthenticated() && isOwner(userId);

      // Users can update their own data, admins can update any
      // Prevent users from changing their own role/verification/status
      // Allow self-service soft delete (status = deleted) with limited fields
      allow update: if isAuthenticated() && (
        (isOwner(userId) && (
          !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role', 'verified', 'status']) ||
          (
            request.resource.data.status == 'deleted' &&
            request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'deletedAt', 'updatedAt'])
          )
        )) ||
        isAdmin()
      );

      // Only admins can delete users
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: DonationHistory
    // ========================================================================

    match /DonationHistory/{userId} {
      // Donors can read/write their own history, admins can manage all
      allow read, write: if isAuthenticated() && (isOwner(userId) || isAdmin());
    }

    // ========================================================================
    // COLLECTION: ReferralTracking
    // ========================================================================

    match /ReferralTracking/{referralId} {
      allow read: if isAuthenticated() && (
        resource.data.referrerUid == request.auth.uid ||
        resource.data.referredUid == request.auth.uid ||
        isAdmin()
      );
      allow create: if isAuthenticated() && request.resource.data.referredUid == request.auth.uid;
      allow update: if isAdmin() || (
        isAuthenticated() &&
        (
          resource.data.referredUid == request.auth.uid ||
          resource.data.referrerUid == request.auth.uid
        ) &&
        isValidReferralStatusUpdate()
      );
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: DonationFeedback
    // ========================================================================

    match /DonationFeedback/{feedbackId} {
      allow read: if isAuthenticated() && (
        resource.data.donorId == request.auth.uid ||
        isAdmin()
      );
      allow create, update: if isAuthenticated() && request.resource.data.donorId == request.auth.uid;
      allow delete: if isAuthenticated() && (
        resource.data.donorId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: donations
    // ========================================================================

    match /donations/{donationId} {
      // Donors can read their own, hospitals can read received, admins can read all
      // Direct donorId check avoids role lookup issues during initial load
      allow read: if isAuthenticated() && (
        resource.data.donorId == request.auth.uid ||
        resource.data.hospitalId == request.auth.uid ||
        isAdmin()
      );

      // Hospitals and admins can create donations
      allow create: if isAuthenticated() && (
        (isHospital() && isVerified()) ||
        isAdmin()
      );

      // Only the creating hospital or admin can update
      allow update: if isAuthenticated() && (
        (isHospital() && resource.data.hospitalId == request.auth.uid) ||
        isAdmin()
      );

      // Only admins can delete
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: bloodRequests
    // ========================================================================

    match /bloodRequests/{requestId} {
      // Anyone authenticated can read active requests
      allow read: if isAuthenticated();

      // Only verified hospitals and admins can create requests
      allow create: if isAuthenticated() && (
        (isHospital() && isVerified()) ||
        isAdmin()
      );

      // Only the requester or admin can update
      // Any authenticated user can update respondedDonors and confirmedDonors arrays
      allow update: if isAuthenticated() && (
        resource.data.requesterId == request.auth.uid ||
        isAdmin() ||
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['respondedDonors', 'confirmedDonors', 'updatedAt'])
      );

      // Only the requester or admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.requesterId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: donorRequests
    // ========================================================================

    match /donorRequests/{requestId} {
      // Only requester, target donor, or admin can read
      allow read: if isAuthenticated() && (
        resource.data.requesterUid == request.auth.uid ||
        resource.data.targetDonorUid == request.auth.uid ||
        isAdmin()
      );

      // Only authenticated donors can create requests for themselves
      allow create: if isAuthenticated() && isDonor() && (
        request.resource.data.requesterUid == request.auth.uid &&
        request.resource.data.targetDonorUid is string &&
        request.resource.data.targetDonorUid != request.auth.uid &&
        request.resource.data.status == 'pending' &&
        request.resource.data.donationType in ['whole', 'plasma', 'platelets'] &&
        (
          request.resource.data.message == null ||
          (request.resource.data.message is string && request.resource.data.message.size() <= 280)
        )
      );

      // Requester can cancel; target donor can accept/reject
      allow update: if isAuthenticated() && (
        (resource.data.requesterUid == request.auth.uid &&
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'updatedAt']) &&
          request.resource.data.status in ['cancelled']) ||
        (resource.data.targetDonorUid == request.auth.uid &&
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'respondedAt', 'updatedAt', 'targetDonorPhone', 'connectionExpiresAt', 'connectionKey']) &&
          request.resource.data.status in ['accepted', 'rejected']) ||
        ((resource.data.requesterUid == request.auth.uid || resource.data.targetDonorUid == request.auth.uid) &&
          isDonorRequestExpiryUpdate()) ||
        isAdmin()
      );

      // Requester or target donor can delete; admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.requesterUid == request.auth.uid ||
        resource.data.targetDonorUid == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: pendingDonorRequests
    // ========================================================================

    match /pendingDonorRequests/{userId} {
      allow read, write: if isAuthenticated() && isOwner(userId);
    }

    // ========================================================================
    // COLLECTION: donorRequestBatches
    // ========================================================================

    match /donorRequestBatches/{batchId} {
      allow read: if isAuthenticated() && (
        resource.data.requesterUid == request.auth.uid ||
        isAdmin()
      );

      allow create: if isAuthenticated() && isDonor() && (
        request.resource.data.requesterUid == request.auth.uid &&
        request.resource.data.donationType in ['whole', 'plasma', 'platelets'] &&
        request.resource.data.targetDonorIds is list &&
        request.resource.data.targetDonorIds.size() > 0 &&
        request.resource.data.targetDonorIds.size() <= 200 &&
        request.resource.data.targetCount == request.resource.data.targetDonorIds.size() &&
        request.resource.data.sentCount == 0 &&
        request.resource.data.deletedCount == 0 &&
        request.resource.data.status in ['sending', 'skipped'] &&
        (
          request.resource.data.message == null ||
          (request.resource.data.message is string && request.resource.data.message.size() <= 280)
        )
      );

      allow update: if isAuthenticated() && (
        (resource.data.requesterUid == request.auth.uid &&
          request.resource.data.diff(resource.data).affectedKeys()
            .hasOnly(['sentCount', 'skippedCount', 'deletedCount', 'status', 'updatedAt', 'skippedTargetIds']) &&
          request.resource.data.status in ['sent', 'skipped', 'failed', 'cancelled']) ||
        (request.auth.uid in resource.data.targetDonorIds &&
          request.resource.data.diff(resource.data).affectedKeys()
            .hasOnly(['sentCount', 'deletedCount', 'status', 'updatedAt']) &&
          request.resource.data.status in ['sent', 'skipped', 'failed', 'cancelled']) ||
        isAdmin()
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: bloodInventory
    // ========================================================================

    match /bloodInventory/{inventoryId} {
      // Anyone authenticated can read inventory (for searching available blood)
      allow read: if isAuthenticated();

      // Only the hospital or admin can create/update their inventory
      allow create: if isAuthenticated() && (
        (isHospital() && isVerified() && hasHospitalEditAccess(request.resource.data.hospitalId)) ||
        isAdmin()
      );

      allow update: if isAuthenticated() && (
        (isHospital() && hasHospitalEditAccess(resource.data.hospitalId)) ||
        isAdmin()
      );

      // Only admin can delete
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: campaigns
    // ========================================================================

    match /campaigns/{campaignId} {
      // Anyone authenticated can read campaigns
      allow read: if isAuthenticated();

      // NGOs and admins can create campaigns
      allow create: if isAuthenticated() && (
        (isNGO() && request.resource.data.ngoId == request.auth.uid) ||
        isAdmin()
      );

      // Only the creating NGO or admin can update
      // Any authenticated user can update registeredDonors and confirmedDonors arrays
      allow update: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        isAdmin() ||
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['registeredDonors', 'confirmedDonors', 'updatedAt'])
      );

      // Only the creating NGO or admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: appointments
    // ========================================================================

    match /appointments/{appointmentId} {
      // Donor and hospital involved, plus admin, can read
      allow read: if isAuthenticated() && (
        resource.data.donorId == request.auth.uid ||
        resource.data.hospitalId == request.auth.uid ||
        isAdmin()
      );

      // Any authenticated user can create appointments
      allow create: if isAuthenticated();

      // Involved parties or admin can update
      allow update: if isAuthenticated() && (
        resource.data.donorId == request.auth.uid ||
        resource.data.hospitalId == request.auth.uid ||
        isAdmin()
      );

      // Only admin can delete
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: volunteers
    // ========================================================================

    match /volunteers/{volunteerId} {
      // NGO and volunteer can read, admin can read all
      allow read: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        resource.data.userId == request.auth.uid ||
        isAdmin()
      );

      // NGO can create volunteers
      allow create: if isAuthenticated() && (
        (isNGO() && request.resource.data.ngoId == request.auth.uid) ||
        isAdmin()
      );

      // NGO or admin can update
      allow update: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        isAdmin()
      );

      // NGO or admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: partnerships
    // ========================================================================

    match /partnerships/{partnershipId} {
      // Involved parties and admin can read
      allow read: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        resource.data.partnerId == request.auth.uid ||
        isAdmin()
      );

      // NGO or admin can create partnerships
      allow create: if isAuthenticated() && (
        (isNGO() && request.resource.data.ngoId == request.auth.uid) ||
        isAdmin()
      );

      // Involved parties or admin can update
      allow update: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        request.resource.data.ngoId == request.auth.uid ||
        resource.data.partnerId == request.auth.uid ||
        isAdmin()
      );

      // NGO owner or admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.ngoId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: notifications
    // ========================================================================

    match /notifications/{notificationId} {
      // Only the recipient or admin can read
      allow read: if isAuthenticated() && (
        resource.data.userId == request.auth.uid ||
        isAdmin()
      );

      // System and admins can create notifications
      // In practice, this should be done via Cloud Functions
      allow create: if isAuthenticated() && (
        isAdmin() ||
        isHospital() ||
        isNGO() ||
        (isDonor() && (
          isReferralNotificationCreate() ||
          isReferralNotificationForReferrer() ||
          isAppointmentNotificationForDonor() ||
          isAppointmentNotificationForHospital() ||
          isBloodRequestNotificationForRequester() ||
          isDonorRequestNotificationForRequester()
        )) ||
        isSelfNotificationCreate()
      );

      // Recipient can update (mark as read), admin can update any
      allow update: if isAuthenticated() && (
        resource.data.userId == request.auth.uid ||
        isAdmin()
      );

      // Recipient or admin can delete
      allow delete: if isAuthenticated() && (
        resource.data.userId == request.auth.uid ||
        isAdmin()
      );
    }

    // ========================================================================
    // COLLECTION: publicDonors (sanitized public donor profiles)
    // ========================================================================

    match /publicDonors/{donorId} {
      allow read: if true;

      allow create, update: if isAuthenticated() && (
        isOwner(donorId) || isAdmin()
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: inventoryTransactions
    // ========================================================================

    match /inventoryTransactions/{transactionId} {
      allow read: if isAuthenticated() && (
        resource.data.hospitalId == request.auth.uid ||
        isHospitalStaffFor(resource.data.hospitalId) ||
        isAdmin()
      );

      allow create: if isAuthenticated() && (
        (isHospital() && hasHospitalEditAccess(request.resource.data.hospitalId)) ||
        isAdmin()
      );

      allow update, delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: inventoryTransfers
    // ========================================================================

    match /inventoryTransfers/{transferId} {
      allow read: if isAuthenticated() && (
        resource.data.fromHospitalId == request.auth.uid ||
        resource.data.toHospitalId == request.auth.uid ||
        isHospitalStaffFor(resource.data.fromHospitalId) ||
        isHospitalStaffFor(resource.data.toHospitalId) ||
        isAdmin()
      );

      allow create: if isAuthenticated() && (
        (isHospital() && hasHospitalEditAccess(request.resource.data.fromHospitalId)) ||
        isAdmin()
      );

      allow update: if isAuthenticated() && (
        isAdmin() ||
        (
          hasHospitalEditAccess(resource.data.fromHospitalId) &&
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'updatedAt'])
        ) ||
        (
          hasHospitalEditAccess(resource.data.toHospitalId) &&
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'updatedAt'])
        )
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: inventoryAlerts
    // ========================================================================

    match /inventoryAlerts/{alertId} {
      allow read: if isAuthenticated() && (
        hasHospitalEditAccess(resource.data.hospitalId) ||
        isHospitalStaffFor(resource.data.hospitalId) ||
        isAdmin()
      );

      allow create: if isAdmin();

      allow update: if isAuthenticated() && (
        hasHospitalEditAccess(resource.data.hospitalId) &&
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['status', 'updatedAt'])
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: inventoryReservations
    // ========================================================================

    match /inventoryReservations/{reservationId} {
      allow read: if isAuthenticated() && (
        hasHospitalEditAccess(resource.data.hospitalId) ||
        isHospitalStaffFor(resource.data.hospitalId) ||
        isAdmin()
      );

      allow create: if isAuthenticated() && (
        hasHospitalEditAccess(request.resource.data.hospitalId) ||
        isAdmin()
      );

      allow update: if isAuthenticated() && (
        hasHospitalEditAccess(resource.data.hospitalId) ||
        isAdmin()
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: bloodbankBranches
    // ========================================================================

    match /bloodbankBranches/{branchId} {
      allow read: if isAuthenticated() && (
        hasHospitalEditAccess(resource.data.parentHospitalId) ||
        isHospitalStaffFor(resource.data.parentHospitalId) ||
        isAdmin()
      );

      allow create: if isAuthenticated() && (
        hasHospitalManagerAccess(request.resource.data.parentHospitalId) ||
        isAdmin()
      );

      allow update: if isAuthenticated() && (
        hasHospitalManagerAccess(resource.data.parentHospitalId) ||
        isAdmin()
      );

      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: badges (Read-only for users)
    // ========================================================================

    match /badges/{badgeId} {
      // Anyone authenticated can read badges
      allow read: if isAuthenticated();

      // Only admins can write badges
      allow write: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: userBadges
    // ========================================================================

    match /userBadges/{userBadgeId} {
      // Anyone authenticated can read badges (for public profiles)
      allow read: if isAuthenticated();

      // Only system/admin can create badges for users
      // In practice, this should be done via Cloud Functions
      allow create: if isAdmin() || (
        isAuthenticated() &&
        request.resource.data.userId == request.auth.uid &&
        request.resource.data.badgeId is string &&
        request.resource.data.earnedAt is timestamp &&
        request.resource.data.keys().hasOnly(['userId', 'badgeId', 'earnedAt'])
      );

      // User can update display preferences, admin can update any
      allow update: if isAuthenticated() && (
        (resource.data.userId == request.auth.uid &&
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['isDisplayed', 'displayOrder'])) ||
        isAdmin()
      );

      // Only admin can delete
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: userStats
    // ========================================================================

    match /userStats/{userId} {
      // Stats can be displayed across the app (leaderboards, dashboards)
      allow read: if isAuthenticated();

      // Allow users to maintain their own stats document, admins can manage all
      allow write: if isAuthenticated() && (isOwner(userId) || isAdmin());
    }

    // ========================================================================
    // COLLECTION: verificationRequests
    // ========================================================================

    match /verificationRequests/{requestId} {
      // Requester and admin can read
      allow read: if isAuthenticated() && (
        resource.data.userId == request.auth.uid ||
        isAdmin()
      );

      // Hospital or NGO can create their verification request
      allow create: if isAuthenticated() && (
        (isHospital() || isNGO()) &&
        request.resource.data.userId == request.auth.uid
      );

      // Requester can update their own request (before review)
      // Admin can always update
      allow update: if isAuthenticated() && (
        (resource.data.userId == request.auth.uid && resource.data.status == 'pending') ||
        isAdmin()
      );

      // Only admin can delete
      allow delete: if isAdmin();
    }

    // ========================================================================
    // COLLECTION: analytics (Admin only write, all can read)
    // ========================================================================

    match /analytics/{analyticsId} {
      // Anyone authenticated can read analytics
      allow read: if isAuthenticated();

      // Only admins can write analytics
      // In practice, this should be done via Cloud Functions
      allow write: if isAdmin();
    }
  }
}
  
